<%= form_with(model: pump_property, local: true) do |form| %>
  <% if pump_property.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pump_property.errors.count, "error") %> prohibited this pump_property from being saved:</h2>

      <ul>
      <% pump_property.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
