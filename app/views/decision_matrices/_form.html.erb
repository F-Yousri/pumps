<%= form_with(model: decision_matrix, local: true) do |form| %>
  <% if decision_matrix.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(decision_matrix.errors.count, "error") %> prohibited this decision_matrix from being saved:</h2>

      <ul>
      <% decision_matrix.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
